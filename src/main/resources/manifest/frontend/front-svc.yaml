apiVersion: v1
kind: Service
metadata:
  name: front-kyotong-service
  namespace: frontend
spec:
  type: NodePort
  selector:
    app: front
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30070